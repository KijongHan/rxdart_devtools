name: rxdart_devtools
description: Dart DevTools extension for inspecting RxDart streams — Subject values, listener counts, emission history.
version: 0.0.1
repository: https://github.com/thomashan/rxdart_devtools
issue_tracker: https://github.com/thomashan/rxdart_devtools/issues

resolution: workspace

environment:
  sdk: ^3.6.0
  flutter: '>=3.27.0'

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^4.9.0
  meta: ^1.16.0
  rxdart: ^0.28.0
  uuid: ^4.5.3

dev_dependencies:
  build_runner: ^2.4.13
  flutter_test:
    sdk: flutter
  json_serializable: ^6.8.0
  lints: ^5.1.0
  test: ^1.25.0
